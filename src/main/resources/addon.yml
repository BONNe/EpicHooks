name: EpicHooks
main: world.bentobox.epichooks.EpicHooksAddon
version: ${version}${build.number}
icon: HOPPER

# Remove dependency as for some reason it may not load before addon.
#depend: [EpicHoppers]

prefix: EpicHooks

authors: [BONNe]
